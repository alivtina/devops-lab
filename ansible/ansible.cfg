# Ubuntu VM uses sudo-rs by default.
# Ansible privilege escalation requires the classic sudo binary.

[defaults]
inventory = inventory.ini

[privilege_escalation]
become = true
become_method = sudo
become_exe = /usr/bin/sudo.ws
